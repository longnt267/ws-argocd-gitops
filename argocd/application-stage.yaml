apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nestjs-demo-stage
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/longnt267/ws-argocd-gitops.git
    targetRevision: main
    path: helm/nestjs-demo-chart
    helm:
      valueFiles:
        - values.yaml
        - values-stage.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: nestjs-stage
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
